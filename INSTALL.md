# 安裝說明

把公關科每天寄來的「榮董新聞」信件，自動整理成 Word 剪報草稿。
原本全手工 40–70 分鐘，改成跑程式約 3 分鐘 + 人工校對。

**這份只講怎麼裝起來、怎麼每天用。** 出狀況要診斷、或想知道程式怎麼判斷的，
看 [`files/README.md`](files/README.md)。

**產出是草稿。程式絕對不會寄信，校對完請自己寄。**

---

## 一、先確認這台電腦有這兩樣

### Python 3

開終端機（Mac）或命令提示字元（Windows），打一行看看：

| | 指令 | 看到版本號就對了 |
|---|---|---|
| Mac | `python3 --version` | `Python 3.12.8` |
| Windows | `py --version` | `Python 3.12.8` |

沒有的話到 <https://www.python.org/downloads/> 裝。
**Windows 安裝時務必勾「Add python.exe to PATH」和「py launcher」**，
漏勾的話等一下會卡在「這台電腦沒有 Python」。

> Windows 補充：直接打 `python` 會叫到 Microsoft Store 的空殼，
> 按下去完全沒反應也不報錯。一律用 `py`。

### Microsoft Word

沒有 Word 也跑得起來，只是**封面目錄的頁碼要自己填**（會維持 `P.__~__`），
其他一切正常。頁碼得等實際排版才知道，所以要借 Word 算一次。

其他 Python 套件**不用自己裝**，第一次執行會自動處理。

---

## 二、拿到程式碼

專案在 <https://github.com/ShibaKen223/obank-clipping>，是 private repo，
所以要先跟管理者拿權限。兩條路：

**A. 用 Git（建議）** — 請管理者把你加成 collaborator，然後：

```bash
git clone https://github.com/ShibaKen223/obank-clipping.git
```

好處是之後程式有更新，你只要在專案資料夾下 `git pull` 就好。

**B. 下載 ZIP** — 不想弄 GitHub 帳號的話，請管理者從 GitHub 頁面
Code → Download ZIP 傳給你，解壓縮即可。缺點是每次更新都要重拿一份。

**不要跟同事直接複製整個資料夾。** 那裡面會有他跑過的信件與剪報成品，
是標「機密／內部限閱」的東西，而且 Python 環境的路徑是寫死的，換一台就壞。

放哪裡都可以，但**選一個之後不會再搬的位置**（例如「文件」底下）。
搬過家的話要重跑一次下面第三步。

---

## 三、安裝（每台電腦只做一次）

在專案資料夾底下執行。

**Mac** —— 開終端機：

```bash
./tools/build_app.sh
```

**Windows** —— 開 PowerShell：

```bash
powershell -ExecutionPolicy Bypass -File .\tools\build_app.ps1
```

> `-ExecutionPolicy Bypass` 是必要的，PowerShell 預設不跑沒簽章的腳本。
> 這只影響這一次執行，**不會改到系統設定**。

跑完桌面會出現一個「**每日剪報**」，這就是之後每天要點的東西。

### Mac 第一次雙擊時會問權限

會跳出「『每日剪報』想要控制『Terminal』」，**按「好」**。
需要終端機是為了讓你看得到 3 分鐘的進度和最後那份摘要，藏起來等於白做。

按到「不允許」的話，之後會一直跳「未獲授權來傳送 Apple Event 到 Terminal
(-1743)」。補救方式：**系統設定 → 隱私權與安全性 → 自動化 → 每日剪報 →
打開 Terminal 開關**。

### 第一次執行會多等約 1 分鐘

它在自動建立 Python 環境、裝套件。只有第一次，之後是瞬間跳過。

---

## 四、每天兩步

1. 在 Outlook / Gmail 把當天的「MMDD_榮董新聞」信件**另存成 `.eml`**，
   丟進「**下載**」資料夾
2. **雙擊桌面的「每日剪報」**

**只丟檔案不會自動開始跑**，一定要雙擊圖示——它不是背景監看程式，是你按了才動。

兩個 `.docx` 附件不用自己存，程式會從信件裡拆。
它會自動挑「下載」資料夾裡**檔名含「榮董新聞」的最新一封**，開頭那行
`信件：0804_榮董新聞.eml` 就是它挑中的，跑之前瞄一眼確認。
想指定別封（例如補跑前一天的），把那個 `.eml` **拖到圖示上**。

### 跑完一定要看視窗最後的摘要

```
完成：outputs/20260804 每日新聞剪報.docx
  成功    29 則（集團新聞 2 / 金融同業新聞 8 / ...）
  待補圖  0 則
  失敗    0 則

  接下來要人工做的:
    1. 校對後自行寄出（本程式不會寄信）
```

「接下來要人工做的」列什麼就做什麼，通常是這幾項：

| 出現時 | 你要做什麼 |
|---|---|
| 待補圖 > 0 | 在 Word 裡搜尋紅字「**圖片待補**」，手動貼圖 |
| 失敗 > 0 | 那幾則**不在 Word 裡**，要自己補。摘要會列出網址和原因 |
| 說要填頁碼 | 這台沒 Word 才會出現，封面的 `P.__~__` 自己填 |
| 每次 | **校對後自己寄出**（程式不會寄信）|

內文提到「王道銀行」「O-Bank」和董事長姓名的地方會**自動標成粗體＋紅色粗底線**，
不用自己標。成品會自動用 Word 開起來，檔案放在 `files/outputs/`。
看完摘要按 Enter 關掉視窗。

---

## 五、卡住的話

| 症狀 | 處理 |
|---|---|
| Mac 一直跳 Apple Event 未獲授權 (-1743) | 見§三那段，去系統設定的「自動化」打開 |
| Windows 說「已停用指令碼執行」 | 你手動跑 `.ps1` 時漏了 `-ExecutionPolicy Bypass`。桌面圖示本身不會遇到 |
| 「存不進 outputs\...docx，檔案正被別的程式鎖住」 | 上一版還開在 Word 裡，關掉再點一次 |
| 「在附件裡找不到集團新聞摘錄檔」 | 公關科改了附件檔名。程式靠檔名認：含「摘錄」的是集團新聞來源，含「每日新聞剪報」的是底稿 |
| 某幾則新聞失敗 | 摘要會列出網址和原因，那幾則要手動補。目前只認得聯合報系、中時集團、自由時報、壹蘋這幾個網域 |
| 搬過專案資料夾之後點了沒反應 | 重跑一次§三的安裝指令 |

其他狀況、或想知道某個判斷為什麼是那樣，看 [`files/README.md`](files/README.md)
的「四、常見狀況排除」和「七、已知限制」。

---

## 六、兩件不要做的事

1. **不要把信件或附件上傳到任何外部服務**（包括雲端硬碟、線上轉檔、AI 工具）。
   信件標註「機密／內部限閱」，整個流程都是本機執行，對外連線只有公開新聞網站。
2. **不要幫程式加自動寄信。** 產出草稿即停是刻意設計的，一定要有人看過才寄。
